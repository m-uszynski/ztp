<Window x:Class="ztp.ProductListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ztp"
        mc:Ignorable="d"
        Title="ProductListWindow" Height="450" Width="800">
    <Grid>
        <ListView Margin="20,50,20,50" Name="Products">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Nazwa" Width="120" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Ilość" Width="50" DisplayMemberBinding="{Binding Count}"/>
                    <GridViewColumn Header="Cena" Width="100" DisplayMemberBinding="{Binding Price}"/>
                    <GridViewColumn Header="VAT" Width="50" DisplayMemberBinding="{Binding VAT}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button x:Name="addProductBtn" Content="+" HorizontalAlignment="Left" Margin="20,15,0,0" VerticalAlignment="Top" Height="30" Width="30" Click="addProductBtn_Click"/>
        <Button x:Name="backToMenuBtn" Content="Wróć do menu" HorizontalAlignment="Left" Margin="20,375,0,0" VerticalAlignment="Top" Width="100" Click="backToMenuBtn_Click"/>
    </Grid>
</Window>
